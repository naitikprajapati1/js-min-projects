AOS.init({
    duration: 1200,
  })

  // nav code
  // create a hamburer menu with
const toggleMenu =()=>{
  console.log('clicking toggleMenu');
  document.body.classList.toggle('active-menu');
}

  // let menu=document.querySelector('.menu-box');
  // let hamburgur=document.querySelector('.ham-burgur');

  // hamburgur.addEventListener('click',()=>{
  //   document.body.classList.toggle('active-menu');
  // })
